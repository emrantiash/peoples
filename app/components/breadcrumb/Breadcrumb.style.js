import commonStyle from "../style/Styles"

const styles ={
    container : {
        backgroundColor : '#fdfdfd',
        width : '55%',
        margin : 0 ,
        // padding : 0
    },
    parentColor : {
        color : commonStyle.color.blue,
        letterSpacing : 0.5
    },
    child : {
        cursor : commonStyle.cursor,
        color :  commonStyle.color.green,
        letterSpacing : 0.5
    }
}

export default styles